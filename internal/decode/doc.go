// Package decode implements the JAB Code read path: binarization and colour
// classification, finder- and alignment-pattern detection, perspective sampling,
// metadata and palette decoding, and the LDPC/demask/deinterleave message decode,
// for primary and docked secondary symbols. The public Decode API lives in the
// parent jabcode package, which wraps Decode here.
package decode
